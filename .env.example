# ============================================================
# CULTIVATED CRM — Environment Variables
# ============================================================
# This project uses per-app env files:
#   apps/web/.env.local    — Next.js web app
#   apps/worker/.env       — Background worker
#
# Copy the respective .env.example in each app directory.
# Root .env is NOT automatically shared between apps.
# (Worker will fall back to root .env if apps/worker/.env is missing.)
# ============================================================

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/cultivated_crm
DIRECT_URL=postgresql://user:password@localhost:5432/cultivated_crm

# Redis
REDIS_QUEUE_URL=redis://localhost:6379/1

# Auth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Ably (server key for publishing + issuing tokens)
ABLY_API_KEY=
# Client fallback (subscribe-only — not needed if /api/ably-token works)
# NEXT_PUBLIC_ABLY_API_KEY=
