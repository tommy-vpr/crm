# apps/web/.env.local

# Database
# For Supabase/PgBouncer: DATABASE_URL = pooled, DIRECT_URL = direct
DATABASE_URL=postgresql://user:password@localhost:5432/cultivated_crm
DIRECT_URL=postgresql://user:password@localhost:5432/cultivated_crm

# NextAuth
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# Google OAuth
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Ably — real-time sync (optional)
# Server key for publishing events from API routes + issuing client tokens
ABLY_API_KEY=your-ably-api-key
# Client fallback key — MUST be subscribe-only (create a restricted key in Ably dashboard)
# Not needed if /api/ably-token works (which it will if ABLY_API_KEY is set above)
# NEXT_PUBLIC_ABLY_API_KEY=your-subscribe-only-key

# Redis (optional — for web-side caching)
REDIS_CACHE_URL=redis://localhost:6379/0

# Email (optional)
RESEND_API_KEY=
EMAIL_FROM=noreply@cultivatedagency.com
